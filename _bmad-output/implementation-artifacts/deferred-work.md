# Deferred Work

## Deferred from: code review of feature-fp-1-2-dynamic-album-quota-and-public-photo-masking (2026-05-11)

- `AlbumController::store` generic `catch (\Exception $e)` returns `error.message = $e->getMessage()` — under a rare concurrent-upload race (two POSTs in the same TX window pass the entitlement check then collide on `unique (face_id, position)`), `Storage::putFileAs` succeeds for the loser, the service catches and cleans up, then rethrows the `QueryException` whose message leaks `SQLSTATE[23000]...` to the API client. Catch was not introduced by this story but its retention is now a known leak path — `backend/app/Http/Controllers/Api/V1/Face/AlbumController.php:69-78`. Tighten by catching `QueryException` specifically and returning a stable French envelope (e.g. `ALBUM_UPLOAD_CONFLICT` 409). Pre-existing.
- `PhotoAlbumService::addPhoto` derives `nextPosition = $currentCount + 1` — non-robust to non-contiguous positions (e.g. `[1, 3, 4]` would compute `4`, collide on the unique constraint, then nondeterministically fail upload). Prefer `(max(positions) ?? 0) + 1` — `backend/app/Services/PhotoAlbumService.php:60`. Pre-existing.
- No retroactive data migration to densify historical `face_photos.position` values. The new masking layer slices by `position <= publicLimit` which silently produces wrong public sets if historical positions have gaps. Audit and backfill — pre-existing data shape concern outside this story. Memory rule `feedback_db_retroactive_migration` applies if remediated.
- `UploadActingVideoRequest::authorize()` 403 (`PREMIUM_REQUIRED`) is rejected from the FormRequest, but the `throttle` middleware on `POST /api/v1/face/acting-video` increments the per-Face counter for every rejected hit. A free Face spamming 21 uploads first sees 403, then flips to 429 after 20 attempts. Pre-existing rate-limit policy; reorder middleware or skip throttle on `HttpResponseException` for stronger UX.
- `FaceEntitlementService::isPremium` re-queries `subscriptions()->active()->...` on every invocation when `activeSubscription` is not eager-loaded. `publicAlbumPhotoLimit` internally re-invokes `isPremium`, so each Face serialization triggers 2 SELECTs. Negligible on single-Face endpoints, becomes 2N on list endpoints (`Producer\CandidatureController` index, future featured-listing). Memoize via per-request cache or require eager-load contract. Perf only — `backend/app/Services/FaceEntitlementService.php:42-52,99-104`.
- `PremiumAlbumMaskingTest::test_downgrade_from_premium_to_expired_preserves_files_and_db_but_masks_public_response` mutates `face_subscriptions` via `FaceSubscription::query()->update([...])` which bypasses Eloquent events/observers. Any future observer (cache invalidation, denormalized premium flag on Face) would not fire in this test, masking regressions. Fragility-only — `backend/tests/Feature/Face/PremiumAlbumMaskingTest.php:174-179`.
- Coverage gap: no test exercises `DELETE /api/v1/face/acting-video` for a free Face whose `acting_video` was uploaded before the premium gate shipped (legacy media). Behaviour is intended (owner can delete masked legacy media) but is not asserted. Test-only — `backend/tests/Feature/Face/PremiumActingVideoMaskingTest.php`.

## Deferred from: code review of fix-26-6-attendance-crons-split (2026-04-29)

- `releaseToFace` / `refundToProducer` silent-noop on null `userId` (Face/Producer User row missing) — `backend/app/Services/MissionPaymentService.php:730-737, 822-829`. Pre-existing pattern; FIX-26.6 inherits the behavior. Auto-validate path can strand a `Locked + Present` entry (mission flips Completed with no FinancialEvent), settle-disputed path will retry the same entry every hour without progressing. FIX-26.6 patches the cron commands defensively (skip + alert on orphan User), but the upstream silent-noop in `MissionPaymentService` should be revisited (throw vs dedicated requeue table). Track as a data-integrity hardening story.
- New hourly crons (`missions:auto-validate-attendance`, `missions:settle-disputed-attendance`) lack `withoutOverlapping()` / `onOneServer()` — `backend/routes/console.php:27-28`. Pattern projet-wide : aucun cron existant n'utilise `withoutOverlapping()`/`onOneServer()` ; à traiter dans une initiative dédiée de hardening scheduling pour éviter une incohérence limitée à FIX-26.6.
- Producer ghosts before `markAttendance` → mission stays `Closed`, never auto-validated by the new `missions:auto-validate-attendance` cron (filter `status = PendingAttendanceValidation`) — `backend/app/Console/Commands/AutoValidateMissionAttendanceCommand.php:44`. Explicitly out of scope per spec § « Coexistence avec AutoReleaseMissionFundsCommand » ; legacy 14-day `AutoReleaseMissionFundsCommand` handles the `Closed` set until FIX-26.10 retires it (FIX-26.10 scope).
- `tryCompleteIfReady` invocations queue `MissionCompletedMail` + create `Notification` rows inside the parent `DB::transaction` rather than via `DB::afterCommit()` — `backend/app/Services/MissionAttendanceService.php:239, 311`. Pre-existing pattern shared with `markAttendance` and `resolveDispute` (FIX-26.3); a transaction rollback after this point would emit user-visible side effects for events that never committed. Service-wide refactor outside FIX-26.6 scope.

## Deferred from: code review of fix-24-4-mission-completed-mail (2026-04-26)

- CTA URL fragile quand `app.frontend_url` est null — pattern `rtrim((string) config('app.frontend_url'), '/').'/...'` partagé par `FaceSelectedMail::buildConfirmUrl`, `FaceConfirmedMail::buildCandidaturesUrl`, `WithdrawalRequestSubmittedMail::adminUrl` et `MissionCompletedMail::buildWalletUrl` produit un CTA relatif non-cliquable dans la plupart des clients mail si la config est vide ; à traiter en sprint email-infra (FIX-24.4 scope ; pre-existing infrastructure pattern, non introduit par cette story)
- Test du chemin `Mail::to(...)->queue(...)` qui throw — aucun test ne simule un échec de queue dans `CompleteMissionTest` pour valider que la complétion mission réussit toujours + crédit wallet OK + notification in-app OK + `Log::warning('MissionCompletedMail queue failed', ...)` émis ; pattern de hardening reproductible depuis FIX-24.3 (test ajouté post-review 2026-04-26) ; non-bloquant car le try/catch est strict-miroir FIX-24.2 (FIX-24.4 scope ; test-only hardening)

## Deferred from: code review of fix-23-1-registration-enabled-deterministic-fallback (2026-04-21)

- Bare `catch` in RegisterProducerPage.vue / RegisterFacePage.vue is broader than the inline comment suggests — swallows 4xx, CORS, JSON parse and TypeError errors in addition to network/5xx/CDN failures (FIX-23.1 scope; comment-vs-reality doc nit, behaviour matches AC #11 table)
- Magic string `Les inscriptions sont temporairement suspendues.` duplicated across RegisterProducerPage.vue, RegisterFacePage.vue and the two new spec files — any i18n/wording change silently breaks regression tests (FIX-23.1 scope; pre-existing copy duplication)
- API returning 200 with a malformed or HTML body (Cloudflare/SSO/502 intercept) leaves `response.data.enabled` `undefined`, and the `v-else-if="!registrationEnabled"` branch then hides the form without firing the `catch`/env fallback (FIX-23.1 scope; pre-existing trust-the-backend assumption)
- API returning string `"true"`/`"false"` for `enabled` is not coerced frontend-side — string `"false"` is truthy in JS and would incorrectly show the form (FIX-23.1 scope; pre-existing)
- `assertHeader('Cache-Control', 'max-age=300, public')` in FaceRegistrationTest.php is brittle — coupled to Symfony's internal directive-order normalization, would fail opaquely if Symfony stops reordering (FIX-23.1 scope; user-approved as the pragmatic framework truth)
- Race condition: component unmount or HMR double-mount before the `onMounted` promise settles — no `AbortController` or mounted guard, last-resolved wins (FIX-23.1 scope; pre-existing, low risk)
- HTTP 401 on the public `/auth/registration-status` endpoint would trip the global 401 response interceptor in `apiClient.ts`, clearing auth and redirecting to `/login?message=session-expired` (FIX-23.1 scope; pre-existing interceptor behaviour, no allow-list)
- Build-time Vite inlining vs runtime `vi.stubEnv` — unit tests exercise the runtime env path, but production bundles get `VITE_REGISTRATION_ENABLED` substituted at `vite build`, giving false confidence that runtime env changes flip behaviour (FIX-23.1 scope; spec-deliberate build-time choice)
- No explicit happy-path test for `{ enabled: true }` resolved response — current suite implicitly covers it via the null→true transition path; AC #11 case 1 not under mechanical lock (FIX-23.1 scope; spec treats it as implicitly covered)
- Pre-fix red/green raw output not captured verbatim in Completion Notes as Task 1.D asked — only a narrative summary is recorded (FIX-23.1 scope; tests re-runnable and green now)

## Deferred from: code review of fix-4-2-admin-featured-faces-ordering (2026-04-05)

- Migration down() does not restore original ville free-text values or quartier data — irreversible migration (FIX-4.1 scope)
- Migration silently nullifies unmatched city values with no audit trail — limited alias coverage in BeninCities::match() (FIX-4.1 scope)
- Frontend/backend Benin city lists are duplicated with no synchronization mechanism — risk of silent divergence (FIX-4.1 scope)
- Validation order: Rule::in + withValidator produce confusing double error messages for ville with non-Benin pays (FIX-4.1 scope)
- normalizedLookup() rebuilds full lookup array on every match() call — no memoization during migration (FIX-4.1 scope)
- loadPage early return on invalid city silently shows empty results with no user feedback (FIX-4.1 scope)
- Test test_formatted_location_with_only_ville relies on pre-existing pays model state (FIX-4.1 scope)
- BeninCities::match() N'Dali apostrophe edge case — "NDali" normalizes to "ndali" which won't match "n dali" (FIX-4.1 scope)
- quartier removed from Face model $fillable but Face factory not verified for stale quartier generation (FIX-4.1 scope)
- useBioLocation validateLocation uses stale bioLocationInfo as fallback for pays — client/server mismatch possible (FIX-4.1 scope)
- Existing city-standardization migration silently nulls unmatched `ville` values with no audit trail (FIX-4.1 scope)
- Admin Face detail page still edits `ville` via free-text input while backend expects the standardized Benin city list (FIX-4.1 scope)
- `quartier` contract removals bundled in adjacent FIX-4.1 changes remain out of scope for FIX-4.2 review patches (FIX-4.1 scope)

## Deferred from: code review of fix-7-2-mission-location-benin-cities (2026-04-06)

- Filter panel `lieu` field in MissionFiltersPanel.vue remains free-text while mission creation now uses enumerated city select — consider converting to select for consistency (FIX-7.2 scope)
- Test fixtures in useMissionsList.spec.ts and AvailableMissionCard.spec.ts use `lieu: 'Paris'` — non-Benin value that doesn't reflect current data model (FIX-7.2 scope)

## Deferred from: code review of fix-8-1-face-age-visibility-toggle (2026-04-06)

- FaceFactory has no `show_age` state helper — tests use inline create(['show_age' => false]), works fine but less explicit (FIX-8.1 scope)
- PublicFaceProfileView casts from list resource lacking age field — pre-existing fragile cast, works via nullish fallback (FIX-8.1 scope)
- Admin panel doesn't surface show_age flag — admins always see age, but can't see what the Face chose (FIX-8.1 scope)

## Deferred from: code review of fix-9-1-whatsapp-editable-profile-completion (2026-04-06)

- No phone number format validation — `whatsapp_number` accepts any string up to 30 chars. Both registration and update flows share this weak validation. Consider adding a regex like `/^\+?[0-9\s\-]+$/` (FIX-9.1 scope)
- Admin cannot edit `whatsapp_number` — `UpdateAdminFaceRequest` does not include the field (FIX-9.1 scope)
- `FaceResource` exposes `whatsapp_number` to all authenticated users — producers can see WhatsApp numbers of all Faces. Consider access control similar to `show_age` (FIX-9.1 scope)
- `FaceFactory::withPersonalInfo()` does not set `whatsapp_number` — future tests using this state may get unexpected completion percentages (FIX-9.1 scope)

## Deferred from: code review of fix-9-2-whatsapp-dashboard-hint-banner (2026-04-06)

- `handlePersonalInfoSave` type signature in `ProfileEditPage.vue` drops `show_age` and `whatsapp_number` — no runtime data loss but TypeScript type is inaccurate (FIX-9.2 scope)

## Deferred from: code review of fix-10-3-no-show-report-wallet-refund (2026-04-07)

- `goBack()` fallback in `FaceBookingDetailPage.vue` sends Producers to `face-dashboard` instead of `producer-dashboard` — pre-existing issue in the shared booking detail page, affects all Producer actions (FIX-10.3 scope)
- `BookingTimeline.vue` references `baseSteps` by array index instead of by key — brittle to future reordering but low risk currently (FIX-10.3 scope)
- `ShouldBroadcastNow` on all booking events adds synchronous latency before HTTP response — cross-cutting concern, all events (BookingCancelled, BookingExpired, etc.) use the same pattern. Consider domain-wide migration to `ShouldBroadcast` (queued) (FIX-10.3 scope)

## Deferred from: code review of rt-1-2-store-frontend-echo-subscription (2026-04-11)

- `unsubscribe()` reads `userId` from auth store at call time — fragile coupling with `clearAuth()` ordering. Storing userId at subscribe-time would be more robust (RT-1.2 scope)
- 401 interceptor in `apiClient.ts` calls `authStore.clearAuth()` but never calls `notificationStore.unsubscribe()` — on session expiry the Echo channel stays subscribed with stale credentials, and `isSubscribed` remains true, preventing re-subscription on next login. Belongs in RT-1.4 fallback/reconnect scope

## Deferred from: code review of rt-1-3-refactor-ui-bell-dropdown (2026-04-11)

- Add a reconciliation path for `unreadCount` after removing polling — defer to RT-1.4 fallback scope, which already covers window focus refresh, reconnect handling, and slow polling recovery
- First dropdown fetch can lose a realtime notification that arrives in-flight — pre-existing RT-1.2 store race around fetch/knownIds replacement; track as a separate store fix rather than patching RT-1.3

## Deferred from: code review of rt-1-4-fallback-non-regression (2026-04-11)

- 401 interceptor removes localStorage tokens synchronously before async `unsubscribe()` runs — if auth store ever derived `user` from localStorage instead of Pinia state, `echo.leave()` would be skipped. Pre-existing interceptor pattern, not introduced by RT-1.4
- No debounce on window focus handler — rapid tab-switching triggers concurrent `fetchUnreadCount()` and `fetchNotifications()` calls, with potential out-of-order response overwrites. Low impact, pre-existing design choice
- In-flight fallback fetches can repopulate the notification store after logout/reset — real but low impact race because only already in-flight requests can write stale badge state, the window is brief, and the next login reconciliation overwrites it
- 401 cleanup can skip `clearAuth()` if the dynamic import chain rejects — defensive weakness in a pre-existing best-effort cleanup pattern; storage is cleared and redirect happens synchronously even if the import chain fails

## Deferred from: code review of fix-19-1-compensate-mission-payment-initiation-failure (2026-04-12)

- Orphan FedaPay transaction after remote `Transaction::create()` succeeds but `generateToken()` or network throws — local deletes payment row, remote transaction persists with stale `custom_metadata.mission_payment_id`; requires reconciliation strategy (fix-19-2/fix-19-4 scope)
- Retry after partial compensation failure blocked by `mission_id` UNIQUE on `mission_payments` — if compensate crashes mid-way, row stays and next `confirmAndPay` hits unique violation → 500; producer trapped (fix-19-2 scope)
- Concurrent double-submit mid-flight creates orphan remote transactions — prepare tx commits before external call, releasing locks; slow FedaPay response lets a second attempt be rejected while the first still spawns a remote transaction (fix-19-2/fix-19-5 scope)
- `Str::uuid()` idempotency key regenerated per call and only stored in FedaPay `custom_metadata` — useless for dedup since FedaPay SDK does not honor it; should derive from `$payment->id` or persist locally (fix-19-2 scope)
- Deadlock risk: prepare locks mission→candidatures→payment; compensate locks payment→mission→candidatures (reverse order) — no retry-on-deadlock wired, fragile under concurrent compensate+retry (fix-19-2 scope)
- Post-commit notification failures silently lose candidature notifications on success path — `notifySafely` swallows failures after `finalizePreparedPayment` commits; producer pays but Faces never learn (pre-existing notifySafely pattern, not introduced by this change)
- Legacy `MissionPaymentService::initiatePayment()` method is currently unused but deferred because fix-19-2 is the planned resume-checkout story and may reuse it as the single resume path

## Deferred from: code review of fix-19-4-log-mission-payment-failure-context (2026-04-14)

- `phase=post_finalize` branch in `MissionPaymentService::handleInitiationFailure` is very hard to reach in practice (the `try` block only spans `requestHostedCheckout`+`finalizePreparedPayment`, and the latter is itself a `DB::transaction`), but kept as a defensive default since a commit-time failure observed after the closure returns is not fully ruled out (FIX-19 follow-up scope)
- `markAsPaid` failure inside `HandleFedapayWebhook::handle` (approved branch) is not wrapped or logged with the new structured context — out of scope for FIX-19.4 (spec only mandates failure-path logging on initiate/resume/compensate/webhook-decline) (FIX-19 follow-up scope)
- Webhook decline/cancel processing leaves local mission/payment state untouched — a producer with a declined transaction stays in `pending_payment` until manual retry or runbook recovery; intentional per spec, not a regression of FIX-19.4 (FIX-19 follow-up scope)

## Deferred from: code review of fix-19-5-cover-mission-payment-failure-regressions (2026-04-14)

- Compensation outcome log assertion in the cleanup-retry test — would catch a regression that skips compensation entirely, but couples the test to log format; log-shape coverage already lives in FIX-19.4 (FIX-19.5 scope)
- Cache lock leak coverage on resume failure — useful guard against a regression deadlocking producer retries, but out of scope for a story meant to lock down only the four named fixes (FIX-19.5 scope)
- Runbook §4.1 SQL `DELETE … WHERE fedapay_transaction_id IS NULL` is a silent no-op for `finalize_local` rows that hold a transaction id — the new "treat as compensate" override redirects operators to §4.1, but its delete won't actually clean such rows. Pre-existing runbook gap surfaced (not introduced) by FIX-19.5 (FIX-19 follow-up scope)
- `fedapay_transaction_id` int/string type asymmetry in the resume test mock (`with(123456)` int vs DB string `'123456'`) — pre-existing pattern already deferred in FIX-19.2 review (FIX-19.2 scope)

## Deferred from: code review of fix-19-2-resume-mission-payment-checkout (2026-04-12)

- `$payment` may be read without definition when `handleInitiationFailure()` returns instead of throwing — `MissionPaymentService.php:60-69`; pre-existing fall-through from FIX-19.1, not caused by this story
- `resolveResumablePayment()` does not `lockForUpdate` when re-reading the mission — `MissionPaymentService.php:246`; the cache lock already serializes producer retries, so the risk is only out-of-band mutation (webhook, admin action) between the read and downstream `initiatePayment()`'s own `lockForUpdate`
- `fedapay_transaction_id` string/int casting between the DB column and FedaPay client is unverified — `MissionPaymentService.php:430` does `(int) $payment->fedapay_transaction_id` while the new test helper seeds the column as string `'123456'`; likely safe via model casts but worth a targeted test in a follow-up

## Deferred from: code review of fix-20-3-remove-legacy-manual-accept-endpoint.md (2026-04-16)

- Paid selection still does not provision conversations for accepted candidatures — `backend/app/Services/MissionPaymentService.php:155`; pre-existing production gap surfaced by FIX-20.3, explicitly deferred to FIX-20.1 rather than patched in this story

## Deferred from: code review of fix-20-6-reconcile-stale-selections-production-data (2026-04-19)

- Unbounded transaction lock hold time + N+1 `User` lookups inside the per-payment transaction — `backend/app/Console/Commands/ReconcileStaleSelectionsCommand.php:292-322, 424-472`; lockForUpdate is held while `resolveFaceUserId` / `resolveProducerUserId` run synchronously for every affected face and producer. Academic for the 181-row prod scope; revisit if the command is ever reused for a larger batch (FIX-20.6 scope)
- Pre-existing `mission_titre` (FR) vs frontend `mission_title` (EN) notification-data key inconsistency — `backend/app/Console/Commands/ReconcileStaleSelectionsCommand.php:440,454,466` vs `frontend/src/features/notification/components/NotificationsDropdown.vue:170`; this story perpetuates the existing `MissionPaymentService` key convention, renderer falls back to `data.message` so the text still shows, only the bold mission header is lost. Cross-cutting fix belongs to a notification-renderer cleanup, not FIX-20.6 (FIX-20.6 scope)
- Dry-run snapshot test diffs only `['id','status']` columns on candidatures/payments/missions — `backend/tests/Feature/Commands/ReconcileStaleSelectionsCommandTest.php:942-946`; a future regression that touched `updated_at` or a sibling column via `->save()` or an observer would still pass the current dry-run test. Current implementation uses bulk updates + marker-exception rollback so actual risk is nil (FIX-20.6 scope)

## Deferred from: code review of fix-22-2-standardize-api-error-format (2026-04-20)

- `WalletController::withdraw` and `Admin/WithdrawalRequestController::approve` catch `RuntimeException` globally and label as `INSUFFICIENT_BALANCE` — any non-balance runtime error (DB, service) is misreported. Pre-existing narrow catch preserved by AC #1/#3 (FIX-22.2 scope)
- Legacy lower_snake error codes (`registration_disabled`, `gender_mismatch`, `active_missions`, `no_user_account`, `self_demotion`, `self_deletion`, etc.) remain unnormalized; `registration_disabled` additionally not present in the `ErrorCodes` enum. Spec AC #7 explicitly re-scopes to follow-up (FIX-22.2 scope)
- No pinning test snapshots the list of preserved lowercase legacy codes — silent refactor drift possible. Recommend adding a guardrail snapshot test (FIX-22.2 scope)
- `Throwable` fallback in `bootstrap/app.php` logs `$e->getMessage()` verbatim without sanitization — potential log hygiene issue if exceptions carry secrets (PDO query with password, FedaPay token). Recommend shared sanitizing log helper (FIX-22.2 scope)
- `FedapayWebhookController` still calls `FedapayWebhookEvent::firstOrCreate(['fedapay_event_id' => ''])` when the event ID is empty — idempotency collapses on first empty-ID event. Pre-existing, surfaced by the refactor (FIX-22.2 scope)

## Deferred from: code review of fix-22-3-centralize-frontend-error-formatter (2026-04-21)

- `formatApiError` rule §1 peut surfacer un champ de validation interne (`_token`, `csrf_token`, etc.) comme premier détail — `getFirstDetailMessage` itère `Object.values(details)` sans filtrer les clés techniques. Pas de preuve que le backend actuel émet de tels champs, mais opportunité de hardening future : skip keys commençant par `_` (FIX-22.3 scope)

## Deferred from: code review of fix-24-2-face-selected-mail.md (2026-04-26)

- Missing payment-entry invariant before applying paid outcomes — pre-existing `markAsPaid()` behavior marks a payment paid and calls `applySelectionOutcomesOnPaid()` without first asserting that the payment has the expected selected entries. If a webhook or self-healing path ever reaches this method with empty/incomplete entries, pending candidatures can be rejected incorrectly. Deferred because FIX-24.2 only adds the Face-selected email hook and explicitly does not refactor `applySelectionOutcomesOnPaid()`.

## Deferred from: code review of fix-26-3-mission-attendance-service (2026-04-28)

- `releaseToFace`/`refundToProducer` promus `public` sans guard runtime `DB::transactionLevel() > 0` — `backend/app/Services/MissionPaymentService.php:723,815`. Le contrat « MUST be called inside DB::transaction() » devient public sans assertion exécutable. MissionAttendanceService respecte la convention, mais tout futur caller pourrait silencieusement persister un état partiel sur rollback. Hardening léger possible (FIX-26.3 scope)
- `markAttendance` retourne un `Mission` via `fresh()` sans eager-load des relations `payment.entries` — `backend/app/Services/MissionAttendanceService.php:161`. Caller HTTP (FIX-26.4) devra `loadMissing` à la sérialisation. Pas de bug, juste un contrat à documenter (FIX-26.4 scope)
- `tryCompleteIfReady` early-return silencieux quand `payment->entries()->exists() === false` (anomalie data) — `backend/app/Services/MissionAttendanceService.php:279-281`. Aucune trace si une mission paid sans entries reste bloquée en `PendingAttendanceValidation`. Ajouter `Log::warning` pour observabilité (FIX-26.3 scope)
- `MissionCompletedMail` queued par `releaseToFace` quand appelé depuis `resolveDispute` (FavorFace) — `backend/app/Services/MissionPaymentService.php:790-796`. La Face reçoit un mail « mission terminée » alors que la mission est complétée depuis des semaines. UX confusing ; à traiter dans FIX-26.5 (notif Face) ou FIX-26.8 (admin resolve), idéalement via un paramètre `$silentMail = false` sur les helpers (FIX-26.5/26.8 scope)
- Migration `notified_at` sans index sur `(attendance_status, notified_at)` — `backend/database/migrations/2026_04_28_130000_add_notified_at_to_mission_payment_candidatures_table.php:14`. La cron FIX-26.6 `settle-disputed-attendance` fera un full-scan. Ajouter l'index dans la story FIX-26.6 où il sera consommé (FIX-26.6 scope)
- Race admin `resolveDispute` ↔ Producer `markAttendance` peut produire double notification `mission_completed_producer` — `backend/app/Services/MissionAttendanceService.php:221-262`. `resolveDispute` ne `Mission::lockForUpdate()` pas avant `tryCompleteIfReady`, donc deux threads peuvent observer `hasUnsettled === false` et flipper la mission en parallèle. Notification table sans contrainte UNIQUE. Fix proposé : ajouter `Mission::lockForUpdate()` au début de la transaction `resolveDispute` (FIX-26.3/26.8 scope, rare en pratique)
- `markAttendance` flippe mission `Closed → PendingAttendanceValidation` même sur batch entièrement no-op (toutes entries `continue`d ligne 133) — `backend/app/Services/MissionAttendanceService.php:150-152`. État atteignable uniquement via anomalie data (entries tranchées sur mission Closed, impossible en flux normal). Patch défensif : ne flip que si au moins une entry a été réellement updated (FIX-26.3 scope)

## Deferred from: code review of fix-26-5-face-absence-notification-and-dispute (2026-04-29)

- Plusieurs `MissionPayment` rows sur la même mission casseraient le lookup `whereHas + first()` du dispute endpoint (non-déterministe) — `backend/app/Http/Controllers/Api/V1/Face/MissionAttendanceController.php:32-35`. Invariant projet « 1 entry par Face par mission » maintenu par construction (`unique candidature_id`), mais à valider en data integrity audit (FIX-26.5 scope)
- Events dispatchés hors transaction firent après rollback si un caller futur wrappe `markAttendance` dans sa propre `DB::transaction` — `backend/app/Services/MissionAttendanceService.php:169-179`. Pas de caller actuel, fragilité architecturale. Pattern projet dispatch hors transaction (cohérent `BookingService::reportNoShow:348-356`) (post-sprint)
- Échec listener silencieux (`Log::warning` puis return) sans retry/réconciliation — `backend/app/Listeners/Mission/SendFaceMarkedAbsentEmail.php:53-58`, `backend/app/Listeners/Mission/NotifyFaceOnAttendanceAbsence.php:55-60`. Face peut ne jamais recevoir notif/email après crash transitoire. Prévoir cron de réconciliation `notified_at IS NOT NULL AND aucune notification existe` en follow-up (post-sprint)
- `Log::warning(..., ['error' => $e->getMessage()])` perd le stack trace — `backend/app/Services/MissionAttendanceService.php:173-177`, listeners idem. Convention Laravel `'exception' => $e` plus diagnosticable (cosmetic)
- `(int) $entry->montant_face_recoit` cast silencieusement `null → 0` produirait email avec « 0 XOF » — `backend/app/Listeners/Mission/SendFaceMarkedAbsentEmail.php:36`. Invariant DB le bloque en pratique, masque les anomalies (cosmetic)
- Aucun test couvrant « dispute sur mission Completed / Closed / paid out » — `backend/tests/Feature/Mission/FaceDisputeAttendanceEndpointTest.php`. Endpoint guarde l'entry (`attendance_status === Absent`), pas le mission status. Spec ligne 41 accepte (entry-state suffit) (post-sprint)
- `disputeDeadline` typed `\DateTimeInterface` puis `Carbon::instance` — `backend/app/Mail/FaceMarkedAbsentMail.php:13,30-33`. Risque tz seulement si caller futur passe `\DateTimeImmutable` UTC ; aujourd'hui les listeners passent du Carbon en tz applicative (cosmetic)
- `Producer::factory()->individual()` vs `->create()` inconsistance entre les 4 tests Mailable — `backend/tests/Feature/Mail/FaceMarkedAbsentMailTest.php:5,28,62,80`. Couvre le cas individual + le cas default ; à uniformiser pour clarté (cosmetic)
- `Log::shouldReceive('warning')->once()` brittle — `backend/tests/Feature/Mission/MissionAttendanceServiceTest.php` (test dispatch failure). Si un autre `Log::warning` fire dans le path test, l'expectation Mockery casse pour raison non liée (cosmetic)
- `assertStringContainsString('/mentions-legales', $rendered)` couple le test à la layout `emails.layouts.base` — `backend/tests/Feature/Mail/FaceMarkedAbsentMailTest.php`. Bris croisé si le layout change (cosmetic)
- Pas de cap longueur sur `mission.titre` ni sanitization CRLF avant insertion dans le sujet email — `backend/app/Mail/FaceMarkedAbsentMail.php:15-17`. Risque header injection théorique (mailers actuels escapent typiquement, mais defense-in-depth absente) (post-sprint)

## Deferred from: code review v2 of fix-26-5-face-absence-notification-and-dispute (2026-04-29)

- Listeners ne checkent pas `attendance_status === Absent` avant de créer la notif / queue le mail — `backend/app/Listeners/Mission/NotifyFaceOnAttendanceAbsence.php`, `backend/app/Listeners/Mission/SendFaceMarkedAbsentEmail.php`. Aujourd'hui sync donc safe (l'event est dispatch immédiatement après le flip vers Absent), mais si listeners passent en `ShouldQueue`, race possible entre dispatch et exécution. Ajouter une garde `if ($entry->attendance_status !== AttendanceStatus::Absent) return;` en tête de listener (post-sprint, à coupler avec le passage queue)
- Listeners non queued (`#[AsEventListener]` sans `ShouldQueue`) — work synchrone dans le request thread Producer. Une `markAttendance` batch sur 50 Faces dispatch 50 events × 2 listeners × ~3 queries chacun = ~300 queries serialisées avant retour HTTP. Considérer `ShouldQueue` pour amortir, surtout après que les gardes `attendance_status === Absent` soient ajoutées (post-sprint, perf optimization)
- Pas de dedup key sur `Notification::create` ni sur `Mail::queue` — forward-compat queue retry. Si un listener queued est replayé après échec partiel, la Face reçoit 2 notifs / 2 emails identiques. À traiter avec `firstOrCreate` keyed sur `(user_id, type, data->>'entry_id')` quand on passera en queued (post-sprint)

## Deferred from: code review of fix-26-7-producer-attendance-validation-ui (2026-04-30)

- Dialog focus management : pas de focus trap, pas de focus initial à l'ouverture, pas de focus restore vers le trigger à la fermeture, listener Escape global au document — `frontend/src/features/mission/components/ValidateAttendanceDialog.vue:258-270, 282-365`. Pattern pre-existing aligné avec `CompleteMissionDialog`. À traiter dans un sweep a11y dédié couvrant tous les dialogs Producer (post-sprint, a11y polish)
- `httpStatus = null` ne distingue pas network-down d'une réponse non-Axios → fetchForm non-Axios error yank le user vers `/missions` sans option retry — `frontend/src/features/mission/composables/useValidateAttendance.ts:864-868`. UX brutale en mobile sénégalais (Orange/Free 3G drops). Couplé à `AttendanceValidationPage.vue:131-136` qui mappe `null status → toast + redirect` (post-sprint, broader offline/retry pattern)
- Component unmount pendant `submitAttendance` pending → `router.push` + `toast.success/error` exécutés sur instance détruite ; pas d'`AbortController` ; pas de guard `isMounted` — `frontend/src/pages/producer/mission/AttendanceValidationPage.vue:150-171`. Vue gère gracefully en prod mais warning en dev + side-effects parasites (post-sprint, async cleanup cross-cutting)
- Banner fieldErrors affiche les clés brutes API (`entries.0.entry_id`) sans mapping label friendly — `frontend/src/pages/producer/mission/AttendanceValidationPage.vue:233-235`. UX dégradée mais 422 rare en happy-path car payload pré-validé côté front (post-sprint, UX polish)
- Spinner SVG sans `role="status"` / `aria-label`, bouton Confirm sans `aria-busy` / `aria-live` pendant submit — `frontend/src/features/mission/components/ValidateAttendanceDialog.vue:343-362`. Pattern pre-existing dans le codebase (à coupler avec sweep a11y dialog ci-dessus) (post-sprint, a11y polish)
- Pas d'`AbortController` sur `fetchForm` → si re-mount rapide ou navigation entre 2 missions sur même route, race possible entre 2 réponses ; la plus tardive overwrite — `frontend/src/features/mission/composables/useValidateAttendance.ts`. Pattern à appliquer aussi à `useCompleteMission` pour cohérence (post-sprint, architecture cross-cutting)
- `getApiErrorDetails` / `getApiErrorMessage` importés depuis `@/features/auth/services/authApi` dans une feature mission → drift architectural — `frontend/src/features/mission/composables/useValidateAttendance.ts:841`. À extraire vers `@/services/apiErrors` partagé (déjà partagé avec `useCompleteMission`) (post-sprint, refactor architecture)
- Route lazy-loaded sans `errorComponent` ni `loadingComponent` → écran blanc si le chunk fail au load — `frontend/src/router/index.ts:180-185`. Pattern pre-existing pour toutes les routes du projet ; à traiter en sweep router (post-sprint, cross-cutting routing concern)
- `formatCurrency` / `formatDate` dupliqués entre `AttendanceValidationPage.vue:34-54` et `ValidateAttendanceDialog.vue:245-256` — drift target garanti. À extraire dans `@/utils/format.ts` partagé (cosmetic, refactor cleanup)
- Submit 409 (mission concurremment complétée) / 404 (mission supprimée) traités par redirect generic vers `/missions` sans toast spécifique — `frontend/src/pages/producer/mission/AttendanceValidationPage.vue:165-170`. UX dégradée mais rare en pratique (post-sprint, UX polish)
- Truncation titre mission à 50 chars peut couper mid-grapheme (accent combiné, surrogate pair, emoji) — `frontend/src/features/mission/components/ValidateAttendanceDialog.vue:238-243`. À utiliser `Intl.Segmenter` ou CSS `line-clamp` (cosmetic, edge UX)
- `<h1>` titre mission sans `truncate` ni `break-words` dans la page → overflow possible sur titres très longs en mobile — `frontend/src/pages/producer/mission/AttendanceValidationPage.vue:217` (cosmetic, UX polish)
- Cas all-non-pending entries (page revisitée après validation) → submit disabled forever sans message ; cas `data.entries === []` aussi sans empty state — `frontend/src/pages/producer/mission/AttendanceValidationPage.vue:251-311`. Invariants backend (mission `Closed` implique entries, statuses pending refusent ces cas) rendent ces edges pratiquement impossibles (post-sprint, defensive UX)
- `getCsrfCookie()` throw → message error vague (« Erreur réseau »), pas de retry guidance — `frontend/src/features/mission/services/missionApi.ts:107-117`. Pattern pre-existing dans le codebase (post-sprint, UX polish)
- `decisions[entry.id]` type-launderé : `Record<number, AttendanceDecision>` retourne `AttendanceDecision` strict alors que les valeurs sont parfois undefined runtime ; le guard `if (decision === undefined) continue` masque ce trou — `frontend/src/pages/producer/mission/AttendanceValidationPage.vue:98-110`. À durcir avec `noUncheckedIndexedAccess` côté `tsconfig.json` (post-sprint, TS config cross-cutting)
- `formatCurrency` `.replace('XOF', '')` fragile vs ICU locales émettant `F\xa0CFA` ou autre → replace no-op silencieux — `frontend/src/pages/producer/mission/AttendanceValidationPage.vue:34-43`. Convention établie dans le codebase, fonctionne sur ICU récent (cosmetic, established convention)
- `markAllPresent` ne donne aucun feedback visuel quand il flippe `decisions[id] = 'present'` sur des entries précédemment marquées `'absent'` — `frontend/src/pages/producer/mission/AttendanceValidationPage.vue:73-78`. Fonctionnellement correct (override silencieux), manque juste un toast info ou highlight transient (cosmetic, UX polish)

## Deferred from: code review v2 of fix-26-7-producer-attendance-validation-ui (2026-04-30, post-patches)

- `@change="decisions[entry.id] = 'present'"` sur radio input ne guard pas `isEntryEditable` côté handler — `frontend/src/pages/producer/mission/AttendanceValidationPage.vue:294, 305`. L'attribut `:disabled` couvre l'UX mais pas l'intégrité data si un screen-reader ou un test programmatique force `setValue`. Defense-in-depth sur action financière définitive (paranoia, post-sprint)
- Toast et dialog lisent `submittedPresentEntries` / `submittedAbsentEntries` en live computed, pas un snapshot au moment d'`openDialog` — `frontend/src/pages/producer/mission/AttendanceValidationPage.vue:340-347`. Si l'user mute une décision via clavier pendant que le dialog est affiché, les compteurs dialog/toast peuvent dériver du payload réellement soumis. Structurellement fragile mais peu probable en pratique (post-sprint)
- `submitAttendance` peut être appelé en parallèle depuis la même instance composable si la dialog reste ouverte sur 422 + reclick rapide — `frontend/src/features/mission/composables/useValidateAttendance.ts:58-86`. Ajouter `if (isSubmitting.value) return Promise.resolve({success:false,...})` en tête. À coupler avec le pattern `AbortController` déjà tracked pour `fetchForm` (post-sprint, architecture)
- `submittablePayload` peut être un tableau vide soumis si race entre clic submit et confirm clear toutes les décisions — `frontend/src/pages/producer/mission/AttendanceValidationPage.vue:150-155`. Backend rejette en 422, mais round-trip évitable via guard `if (submittablePayload.value.length === 0) return` (post-sprint, rare)
- Récap bas-de-page (cumul mission) `2 Face(s) présente(s) — 180 000 XOF versés` n'est pas labellisé explicitement « Cumul mission » → producer pourrait penser que ce récap reflète son action courante alors qu'il inclut les hydratés — `frontend/src/pages/producer/mission/AttendanceValidationPage.vue:313-323`. Décision review D1 option 3 : page = cumul, dialog/toast = this-action. Manque label explicite côté UI (post-sprint, UX clarity polish)
- `toast.success` immédiatement suivi de `router.push({ name: 'producer-missions' })` → toast peut être démontés par le router avant lecture selon implémentation `useToast` — `frontend/src/pages/producer/mission/AttendanceValidationPage.vue:158-161`. Couplé au toast manager (post-sprint, UX polish)
- Route `producer-mission-attendance` à path `missions/:id/attendance` n'a pas de regex UUID constraint sur `:id` → URL garbage rend la page puis 404 server-side — `frontend/src/router/index.ts:180-185`. Pattern pre-existing (toutes les routes mission sans constraint) (post-sprint, defensive routing)

## Deferred from: code review v3 of fix-26-7-producer-attendance-validation-ui (2026-04-30, post-v2)

- Surrogate pair / combining accent corruption sur `truncatedTitle.substring(0, 50)` (Dialog) et `display_name.charAt(0)` (avatar) — `frontend/src/features/mission/components/ValidateAttendanceDialog.vue:249-254`, `frontend/src/pages/producer/mission/AttendanceValidationPage.vue:1433`. À utiliser `Array.from(str)[0]` ou `Intl.Segmenter` ou CSS `line-clamp` pour grapheme-safe (post-sprint, edge UTF-16 polish)
- Banner fieldErrors `<div v-for>` rend un wrapper vide si `messages.length === 0` (le `v-if` interne sur `<template>` n'empêche pas le `<div>` parent de s'afficher) — `frontend/src/pages/producer/mission/AttendanceValidationPage.vue:233-237`. Filtrer au niveau du `v-for` via un computed `filteredFieldErrors` (post-sprint, cosmetic — cas backend rare)
- Mission `completed` n'expose aucun chemin pour consulter en read-only le détail des présences déjà tranchées (audit trail post-validation) — `frontend/src/features/mission/components/MissionCard.vue:26-30`. Hors scope FIX-26.7. À étudier dans FIX-26.8 ou story future « audit attendance read-only » (post-sprint, dead-end UX bouclage)
- `flex-wrap` sur la actions row de `MissionCard` peut produire un visuel mal aligné quand la première ligne contient le bouton `flex-1 md:flex-none` (Voir candidatures) et que les suivants wrappent en row 2 sans `flex-1` — `frontend/src/features/mission/components/MissionCard.vue:46-47`. UX visuel mineur sur very narrow viewports (post-sprint)
- `MissionsListPage.spec.ts` mock du barrel `@/features/mission/composables` shadow tous les exports → fragile si la page importe un nouveau composable du même barrel à l'avenir — `frontend/src/pages/producer/mission/__tests__/MissionsListPage.spec.ts:2051-2068`. Brittleness test (à durcir avec import sélectif via `vi.importActual` + override) (post-sprint, test polish)
- `watch(missionUuid, loadForm, { immediate: true })` ne wrap pas `loadForm` (async) dans un IIFE try/catch → unhandled promise rejection silencieux si une exception synchrone se glisse pré-fetchForm — `frontend/src/pages/producer/mission/AttendanceValidationPage.vue:1331`. Defensive (pas de throw aujourd'hui en pré-fetchForm) (post-sprint, defensive)
- Radio row inner `<div class="flex items-center gap-3">` pour `Présente`/`Absente` n'a pas `flex-wrap` — viewport <320px peut overflow horizontal sur le row interne — `frontend/src/pages/producer/mission/AttendanceValidationPage.vue:1448-1471`. UX edge mobile très étroit (post-sprint, defensive)
- Test composable `« state reset across calls »` couvre `fetchForm`-chain ; pas de test pour `submitAttendance` après un `fetchForm` 422 (régression silencieuse possible si le reset intra-`submitAttendance` était cassé) — `frontend/src/features/mission/composables/__tests__/useValidateAttendance.spec.ts`. Coverage gap mineur (post-sprint, test coverage)
- Test MissionCard whitelist couvre `pending_payment` et `completed` avec `has_paid_payment: true`, mais pas `pending_attendance_validation` avec `has_paid_payment: false` (régression silencieuse possible si la garde `has_paid_payment` était droppée pour ce statut spécifique) — `frontend/src/features/mission/components/__tests__/MissionCard.spec.ts:467-487`. Coverage gap mineur (post-sprint, test coverage)
- Disputed entry hydratée comme `'absent'` mais `attendance_status_label = 'Contestée'` n'est jamais affiché visuellement sur la page (Producer ne distingue pas absent vs disputed dans la liste) — `frontend/src/pages/producer/mission/AttendanceValidationPage.vue:1175-1178`. La mention `notified_at — fenêtre contestation 72h` couvre déjà la 72h info, mais le label `Contestée` reste invisible (post-sprint, UX clarity)

## Deferred from: code review of fix-26-7-producer-attendance-validation-ui (2026-04-30, v3)

- MissionCard `formatDate()` does not guard invalid date strings — `frontend/src/features/mission/components/MissionCard.vue:37-43`. Pre-existing formatter behavior in MissionCard, not introduced by the attendance CTA; consider adding the same invalid-date guard used by `AttendanceValidationPage` in a general MissionCard hardening pass.

## Deferred from: code review of fix-26-8-admin-attendance-disputes-resolution (2026-05-01)

- `releaseToFace` silently returns when the Face has no linked User — `backend/app/Services/MissionPaymentService.php:731`. This was pre-existing helper behavior before FIX-26.8, but the new admin resolve path inherits it: anomalous data can make a favor-Face resolution report success while escrow remains locked. Harden in a financial helper follow-up by throwing a validation/domain exception instead of returning.
- `refundToProducer` silently returns when the Producer has no linked User — `backend/app/Services/MissionPaymentService.php:827`. This was pre-existing helper behavior before FIX-26.8, but the new admin resolve path inherits it: anomalous data can make a favor-Producer resolution report success while escrow remains locked. Harden in the same financial helper follow-up.

## Deferred from: code review of fix-26-9-legacy-attendance-migration (2026-05-02)

- TOCTOU race pre-pivot path : `findOrphanFaceIds` et `findNonPendingLockedEntryIds` runnent hors per-mission transaction ; `MissionService::completeMission` ne re-vérifie pas `status === Closed` sous `lockForUpdate`. Race possible avec `AutoReleaseMissionFundsCommand` (cron daily encore actif tant que FIX-26.10 backlog) ou avec une action admin entre fetch et processing — `backend/app/Console/Commands/LegacyAttendanceSettlementCommand.php:124,143,162`. Risque opérationnel faible (commande one-shot manuelle, fenêtre courte). À harden si la commande devient récurrente.
- Aucun test n'exerce les branches `failed:legacy_exception` / `failed:post_pivot_exception` (compteur `failed > 0`) — `backend/tests/Feature/Commands/LegacyAttendanceSettlementCommandTest.php`. Si l'incrément typote `++$skipped` au lieu de `++$failed`, aucun test n'attrape. Defer test polish.
- `findOrphanFaceIds` couvre seulement Face, pas Producer — si User Producer supprimé, `MissionService::notifyProducerOnCompletion` retourne silencieusement et la mission est settled sans notification audit-trail — `backend/app/Console/Commands/LegacyAttendanceSettlementCommand.php:298`. Edge case, pas de fixture test.
- Mass update post-pivot `Mission::where(...)->update(...)` bypass les events Eloquent (`updating`/`updated`). Pas d'observer Mission aujourd'hui (vérifié grep), mais à re-considerer si un observer est introduit plus tard — `backend/app/Console/Commands/LegacyAttendanceSettlementCommand.php:217`.
- Tests cutoff boundary imprécis — aucun test à `date_tournage = now()->subHours(95)` (exclu attendu) vs `subHours(97)` (inclu attendu) — `backend/tests/Feature/Commands/LegacyAttendanceSettlementCommandTest.php:553-570`. Régression sur opérateur `<` vs `<=` ou sur valeur `subHours(96)` non détectable.
- Mid-loop fatal abort observability — pas de catch global autour du `foreach`. Une `\Error` PHP fatale (ex: `created_at` null sur une mission corrompue) interrompt la boucle et le footer summary n'est jamais imprimé, opérateur aveugle sur l'avancement réel — `backend/app/Console/Commands/LegacyAttendanceSettlementCommand.php:92-117`.
- Catch `ValidationException` sur le path pre-pivot est dead code — `MissionService::completeMission` throw `\RuntimeException` (pas `ValidationException`). Le catch ligne 171 ne sera jamais déclenché en pratique. Pourrait être nettoyé pour clarté mais inoffensif — `backend/app/Console/Commands/LegacyAttendanceSettlementCommand.php:171`.
- Pivot date timezone ambiguity sur ISO avec offset — `Carbon::parse('2026-05-01T00:00:00+09:00')` accepté avec offset préservé, miscomp avec `created_at` UTC. Mitigé en pratique par P1 (validation regex stricte `^\d{4}-\d{2}-\d{2}$` refusera ISO avec time/tz) — `backend/app/Console/Commands/LegacyAttendanceSettlementCommand.php:48`.

## Deferred from: code review of feature-fp-1-5-annual-payment-initiation-and-idempotent-activation (2026-05-12)

- Duplicate concurrent Fedapay webhook envelopes can race on `FedapayWebhookEvent::firstOrCreate` before envelope-level idempotency applies — `backend/app/Http/Controllers/Api/V1/FedapayWebhookController.php:48`. This is a pre-existing webhook-controller issue, not introduced by FP-1.5. Future hardening: use `createOrFirst()` if available, or catch duplicate-key `QueryException` and re-read the existing event before the processed-status check.

## Deferred from: code review v2 of fix-26-9-legacy-attendance-migration (2026-05-02)

- Pre-pivot orphan check (`findOrphanFaceIds`) runs outside the per-mission DB::transaction. Face User row deletion is rare in production, and the downstream `releaseToFace` silently no-ops on missing User which is the existing legacy behavior — already tracked in the `fix-26-8` deferred-work entry as a financial helper hardening follow-up. The legacy command inherits this exposure but does not introduce new risk — `backend/app/Console/Commands/LegacyAttendanceSettlementCommand.php:298`.
- `test_apply_counts_failed_when_mission_service_throws` only asserts the `failed=1` counter, the structured `Failed to settle mission #X` console line, and that the mission status remains `Closed` after rollback. It does NOT assert (a) `Mail::assertNothingQueued()` for the failed mission, (b) entry-level rollback (`escrow_status = Locked`, `attendance_status = Pending`), (c) `WalletTransaction::count() === 0`, (d) absence of orphan `FinancialEvent` rows. Future regression in `MissionService::completeMission` that issues partial side-effects before throwing would not be caught by this test — `backend/tests/Feature/Commands/LegacyAttendanceSettlementCommandTest.php:test_apply_counts_failed_when_mission_service_throws`.

## Deferred from: code review of feature-fp-1.1-subscription-schema-and-entitlement-service (2026-05-11)

- `FaceEntitlementService::isPremium()` and `FaceSubscription::isActive()` do not require `starts_at <= now()`. Spec AC #6 only specifies `expires_at`, but a pre-effective active row (admin pre-provisioning, backfill, scheduled future activation) immediately grants premium today — `backend/app/Services/FaceEntitlementService.php`, `backend/app/Models/FaceSubscription.php`. Owner: FP-1.X admin/payment activation story.
- Mass-assignment surface on `FaceSubscription::$fillable` (face_id, plan, status, paid_amount, provider, provider_reference, metadata, dates). Spec AC #4 mandates this shape, so all FP-1.X payment/admin callers MUST use Form Requests / DTOs and never `Model::create($request->all())` — `backend/app/Models/FaceSubscription.php`. Owner: FP-1.X payment + admin stories.
- `paid_amount` integer cast is vulnerable to raw webhook input: float strings like `"99500.50"` truncate silently to 99500; negative numeric strings raise `QueryException` against the unsigned column (webhook retries indefinitely); `"NaN"` coerces to 0 and corrupts financials. Validate at the webhook Form Request boundary — `backend/app/Models/FaceSubscription.php`. Owner: FP-1.X payment ingestion story.
- `metadata` `array` cast silently serializes non-array writes (`$sub->update(['metadata' => 'oops'])` becomes a JSON string) and breaks the `array<string,mixed>` PHPDoc contract on read — `backend/app/Models/FaceSubscription.php`. Owner: FP-1.X webhook persistence story.
- `starts_at`, `expires_at`, `cancelled_at` declared as MySQL `dateTime` (timezone-naive). All current comparisons bind PHP `now()` and are safe, but any future DB-side time expression (`NOW()`, `DATE_SUB`, default-current, scheduled cleanup query) would diverge if the MySQL session timezone differs from `config/app.php` — `backend/database/migrations/2026_05_11_083606_create_face_subscriptions_table.php`. Owner: architecture pass (document UTC convention or migrate to `timestamp`).
- Test coverage gaps: (a) scope-vs-helper boundary tested on different rows — a refactor changing `scopeActive` to `>=` would not surface; (b) `provider_reference: null → 'fedapay_xxx'` lifecycle (typical PendingPayment → Active webhook update on the same row) is not exercised — `backend/tests/Feature/Face/FaceSubscriptionSchemaTest.php`. Owner: FP-1.X webhook story hardening pass.
- `FaceSubscriptionFactory::pendingPayment()` and `failed()` keep `paid_amount` between 10k and 200k XOF — semantically wrong (no payment was collected). Tests pass because the service ignores `paid_amount`, but fixtures mislead future authors writing payment/financial-event assertions — `backend/database/factories/FaceSubscriptionFactory.php`. Owner: factory hygiene pass before FP-1.X payment stories.
- No partial unique index on `(face_id, status='Active' [AND plan=AnnualPremium])`. The `Face::activeSubscription` HasOne patch above hardens the read path, but the schema still permits two concurrent Active rows for one Face if webhooks double-fire or admin operations race. Spec line 240 explicitly defers this concurrency guarantee to "payment/admin stories that should handle them transactionally" — `backend/database/migrations/2026_05_11_083606_create_face_subscriptions_table.php`. Owner: FP-1.X payment idempotency / admin transactional story.

## Deferred from: code review v2 of feature-fp-1.1-subscription-schema-and-entitlement-service (2026-05-11)

- Asymmetric defensive checks in `FaceEntitlementService::resolveActivePremiumSubscription` — preloaded branch re-runs `qualifiesAsPremium($candidate)` to defend against expiry crossing mid-request, while the query branch returns `->first()` directly without the same re-check. Race window microscopic, but harden for principled symmetry when a downstream caller starts reading `expires_at` from the returned model — `backend/app/Services/FaceEntitlementService.php:80-94`. Owner: FP-1.X service consumer story (e.g. subscription status endpoint, FP-1.6).
- `Face::subscriptions()` HasMany has no default ordering — `$face->subscriptions->first()` returns DB insertion order, not "most recent active". No consumer today; add `->latest('expires_at')` or document the "always go through `activeSubscription` or `FaceEntitlementService`" convention in PHPDoc before stories bypass the helper — `backend/app/Models/Face.php`. Owner: hardening pass before any story reads `subscriptions` directly.
- Composite index `(face_id, status, expires_at)` does not include `plan`. Performance is fine today (single plan, low row count), but selectivity drops once a second plan ships (e.g. `MonthlyPremium`) or after years of subscription churn. Re-check the query plan and consider widening to `(face_id, plan, status, expires_at)` — `backend/database/migrations/2026_05_11_083606_create_face_subscriptions_table.php`. Owner: revisit when adding a 2nd plan or when `face_subscriptions` grows past ~10k rows.
- `expires_at` / `starts_at` / `cancelled_at` are declared as MySQL `dateTime` (1-second resolution). A webhook double-fire within the same wall-clock second produces identical timestamps on both rows; `unique(provider_reference)` is the only DB-level dedup for FedaPay-style references, but NULL-`provider_reference` rows (admin/manual grants) have no guard. Consider either `dateTime('expires_at', 3)` for sub-second precision or `unique(face_id, provider_reference)` for tighter replay defense — `backend/database/migrations/2026_05_11_083606_create_face_subscriptions_table.php`. Owner: FP-1.X payment idempotency / admin grant story.
- `HasRouteUuid` trait integration on `FaceSubscription` is not tested — every fixture pre-fills `uuid` explicitly via the factory, so no test proves the trait actually populates `uuid` on `create()` without explicit input. Add a `factory()->state(['uuid' => null])->create()` assertion or remove the explicit `uuid` from the factory definition and assert the trait fills it — `backend/database/factories/FaceSubscriptionFactory.php`, `backend/app/Models/FaceSubscription.php`. Owner: factory hygiene / trait coverage pass.
- No DB-level `CHECK` constraint or enum column type on `plan` and `status` — if a row with an unknown plan value ever survives in the DB (legacy import, migration drift, future enum reduction), `$face->subscriptions->first()` crashes on Eloquent enum cast hydration. The new test deliberately uses raw `DB::table` insert to validate that the `activeSubscription` filter excludes such rows, but the broader hydration surface is exposed — `backend/database/migrations/2026_05_11_083606_create_face_subscriptions_table.php`. Owner: architecture decision on schema-level enum enforcement (MySQL ENUM column vs. CHECK constraint vs. application-level defense in casts).

## Deferred from: code review of feature-fp-1.3-face-subscription-status-and-entitlement-api (2026-05-11)

- Orphan Face: `findOrFail($user->userable_id)` throws `ModelNotFoundException` for an authenticated Face whose Face row was hard-deleted, returning the generic 404 envelope rather than a typed `FACE_NOT_FOUND` error — `backend/app/Http/Controllers/Api/V1/Face/SubscriptionStatusController.php:34-36`. Data-integrity edge with no production fixture; consider a typed envelope when the broader User↔Face orphan policy is addressed.
- `FIELD()` SQL function couples representative subscription resolver to MySQL — `backend/app/Http/Controllers/Api/V1/Face/SubscriptionStatusController.php:96-99`. Spec resolves ambiguity in favour of `FIELD()` knowing the test DB is MySQL, but a dev switching their local test DB to SQLite hits SQL syntax errors on every fallback test. Portable alternative: PHP-side sorting after `->get()` (small N per Face).
- Corrupt `status` enum row crashes Eloquent hydration (500) — `backend/app/Http/Controllers/Api/V1/Face/SubscriptionStatusController.php:36-43`. Pairs with the FP-1.1 deferred item "No DB-level CHECK constraint or enum column on plan/status". Right fix surface is schema-level (MySQL ENUM column / CHECK constraint), not the controller.
- Corrupt `plan` enum row crashes fallback path (500) — `backend/app/Http/Controllers/Api/V1/Face/SubscriptionStatusController.php:95-100`. Same family as the corrupt-status defer; covered by the same schema-level enum-enforcement decision.
- Eager-load query-count guardrail threshold is loose (`assertLessThanOrEqual(2, ...)`) — `backend/tests/Feature/Face/SubscriptionStatusTest.php:370-388`. Intentional per Task 4.20 ("absorb fixture queries"), but a regression that adds 1 extra subscription SELECT (1 → 2) would not fail. Tightening to `assertCount(1, ...)` after stabilising fixture overhead is the harder regression net.
- No test for terminal `id DESC` tie-breaker when two rows share identical `created_at` — `backend/tests/Feature/Face/SubscriptionStatusTest.php`. Secondary sort in `FIELD(status, ?) DESC, created_at DESC, id DESC` is exercised only by the `created_at` axis.
- AC #4 `starts_at`/`expires_at` asserted only as non-null (`assertNotNull`) — `backend/tests/Feature/Face/SubscriptionStatusTest.php:117-118`. Story-internal inconsistency: Task 4.4 mandates `assertNotNull`, while AC #4 mandates exact ISO-8601 string equality. Implementation follows Task 4.4 as delivered; strengthen if the AC's exact-string contract becomes load-bearing for FP-1.5/FP-1.7.
- No `assertJsonStructure` over the full success payload — `backend/tests/Feature/Face/SubscriptionStatusTest.php`. `test_response_uses_data_envelope_only` checks only the top-level `['data']` key. Accidental drop of a sub-key (`is_acting_video_publicly_visible`, `annual_plan.provider`, …) would slip through every state test.
- Empty-string currency/provider when config explicitly nulled at runtime — `backend/app/Http/Controllers/Api/V1/Face/SubscriptionStatusController.php:73-74`. `(string) config(...)` on `null` returns `""`. Defaults in `config/face_premium.php` (`'XOF'`/`'fedapay'`) prevent the env-missing case, so only an explicit `config([... => null])` exposes blank strings. Defensive `?? 'XOF'` / `?? 'fedapay'` fallback is cheap insurance.

## Deferred from: code review v2 of feature-fp-1.3-face-subscription-status-and-entitlement-api (2026-05-11)

- `whereIn` allowlist creates forward-compat hazard if `FaceSubscriptionStatus` gains a new case (e.g. `Refunded`, `Suspended`, `Chargeback`) — `backend/app/Http/Controllers/Api/V1/Face/SubscriptionStatusController.php:96-101`. Defensive alternative: `whereNot('status', Active)` (denylist auto-includes new cases). Acceptable as-is since adding a status case is a deliberate schema change that would touch this controller anyway. Owner: revisit when FP-1.X introduces a refund/chargeback status.
- No test combines "pending_payment recent + expired older" to explicitly pin the `FIELD(status, 'pending_payment') DESC` priority in the resolver fallback — `backend/tests/Feature/Face/SubscriptionStatusTest.php`. Existing coverage tests pending-alone, expired-alone, active-vs-terminal, and recent-cancelled-vs-old-expired, but not the pending-priority axis. A regression dropping the FIELD() priority would silently let the older expired row win for a pending-payment Face. Owner: test coverage polish pass.

## Deferred from: code review of feature-fp-1.4-admin-subscription-operations-and-audit-trail (2026-05-11)

- DB-level append-only enforcement for `face_subscription_audits` (triggers, READ ONLY, observer-blocking `saving`/`deleting` events) — `backend/app/Models/FaceSubscriptionAudit.php`. `const UPDATED_AT = null` and the absence of a mutation endpoint are convention only; nothing prevents a future code path from calling `$audit->save()` or `FaceSubscriptionAudit::where(...)->update(...)`. Spec line 221 explicitly defers DB-level enforcement. Owner: revisit if legal/compliance review demands tamper-resistance proof at the DB layer.
- Admin subscription `index` endpoint not paginated — `backend/app/Http/Controllers/Api/V1/Admin/AdminFaceSubscriptionController.php:26-43`. AC #7 envelope deliberately omits `meta`. A Face with N years of renewals + corrections returns all subscriptions and all audits eagerly. Owner: revisit when the largest face's audit count exceeds ~50.
- Timezone handling on admin-supplied `starts_at`/`expires_at` — `backend/app/Services/FaceSubscriptionAdminService.php:51`, `backend/app/Http/Controllers/Api/V1/Admin/AdminFaceSubscriptionController.php:51,122-125`. `Carbon::parse` preserves the admin-supplied offset; MySQL `DATETIME` then stores it in the app timezone, potentially drifting from the admin's intent. Project-wide concern, not introduced by this story. Owner: define a project-level timezone convention (UTC at the boundary?).
- No structured field for offline payment metadata in `activate()` — `backend/app/Services/FaceSubscriptionAdminService.php:54-66`. Manual activations store `paid_amount=null`, `currency='XOF'`, `provider=null`, `provider_reference=null`; the only trace of the offline transaction is the audit `notes` free-text. Owner: FP-1.5 should introduce a structured offline-receipt shape (e.g. `provider='manual'` + `provider_reference='MTN-DEPOT-XXX'` + `paid_amount` populated).
- `extend`/`cancel`/`correct` call `$locked->fresh()` twice — `backend/app/Services/FaceSubscriptionAdminService.php:108,111,142,145,175,178`. Minor perf (extra round-trip per call). Could be refactored to a single `refresh()` or by trusting the in-memory model after `save()`. Owner: low-priority cleanup.
- `face_subscription_audits.notes` column has no DB-level minimum length — `backend/database/migrations/2026_05_12_000000_create_face_subscription_audits_table.php:24`. The `min:5` rule is enforced at the FormRequest only; any path that bypasses the request (factories, seeders, future console commands) can insert short or empty notes. Owner: revisit when audit data integrity becomes a compliance requirement.
- Laravel `date` rule accepts ambiguous strings (`now`, `tomorrow`, `next monday`) — `backend/app/Http/Requests/Admin/ActivateFaceSubscriptionRequest.php:30`, `backend/app/Http/Requests/Admin/CorrectFaceSubscriptionRequest.php:33-34`. Two clients sending the same wall-clock date in different formats can produce subtly different snapshots. Owner: consider `date_format:Y-m-d\TH:i:sP` (strict ISO 8601) for admin date inputs project-wide.

## Deferred from: code review v2 of feature-fp-1-5-annual-payment-initiation-and-idempotent-activation (2026-05-12)

- Request-checkout failure intentionally preserves a `pending_payment` row with `provider_reference = null` when Fedapay throws before returning a transaction id — `backend/app/Services/FaceSubscriptionPaymentService.php:114-132`. This avoids orphaning a possible remote transaction when the HTTP response is lost after Fedapay commits, but it blocks Face self-service retry if no remote transaction exists. Decision on 2026-05-12: keep preserve-row behavior; add stale-pending cleanup/admin recovery for null-provider pending rows.
- `Cache::lock()->block()` timeout path releases in `finally` even when this process may not own the lock — `backend/app/Services/FaceSubscriptionPaymentService.php:174`. Current code mirrors the existing Mission payment lock pattern; defer cross-service hardening to track acquisition explicitly before release.
- Compensation failure leaves an orphan `PendingPayment` row with no automated cleanup — `backend/app/Services/FaceSubscriptionPaymentService.php:286-310`. If `compensateFailedInitiation()` itself fails (deadlock, connection drop), the row stays in `pending_payment` and blocks future `initiate()` calls via `pendingPaymentExists()` 409. Recovery currently requires admin intervention via FP-1.4 endpoints. Owner: add a scheduled cleanup job for stale pending rows (e.g. `pending_payment` older than 24h with null `provider_reference`).
- `finalizePendingWithRetry` retries on any `\Throwable` with no classification — `backend/app/Services/FaceSubscriptionPaymentService.php:321-347`. Permanent failures (schema constraint violations, OOM, code bugs) get retried 3×50ms before re-throw, wasting CPU and log noise. Story explicitly designed for "preserve and let webhook recover" so the cost is just retry waste, not correctness. Owner: add a retryable-exception allowlist (DeadlockException, LockTimeoutException, QueryException with connection error codes).
- `markAsPaid` chaining lookup `lockForUpdate()` + `orderByDesc` may take gap locks on historical Active rows under MySQL — `backend/app/Services/FaceSubscriptionPaymentService.php:194-203`. Bounded in practice because the `expires_at > now()` clause restricts to ~1 row by invariant, but a defective state with multiple Active rows could lock more than intended. Owner: revisit if multi-Active-row anomalies appear in production.
- `provider_reference` lookup vs storage requires consistent integer→string coercion at both ends — `backend/app/Jobs/HandleFedapayWebhook.php:218-220`, `FaceSubscriptionPaymentService.php:367`. A Fedapay payload variation (leading-zero string, scientific notation, etc.) would cause the primary lookup to miss and force the AC #12bis fallback. Mission flow has the same pattern. Owner: revisit if a Fedapay payload shape change is observed.
- `(int) $transaction->id` cast in `FedapayService::initiatePaymentForFaceSubscription` mirrors the pre-existing booking/mission pattern — `backend/app/Services/FedapayService.php:144`. Codebase-wide change needed if this is to be hardened. Owner: cross-cutting refactor.
- `Face::findOrFail($user->userable_id)` returns 404 instead of typed 403 for stale `userable_id` (Face row hard-deleted under load) — `backend/app/Http/Controllers/Api/V1/Face/SubscriptionPaymentController.php:35`. Same pattern as FP-1.3 `SubscriptionStatusController:34-36`. Owner: aligned with the broader User↔Face orphan policy.
- `currency` config value > 3 chars breaks MySQL strict-mode insert — `backend/app/Services/FaceSubscriptionPaymentService.php:96`. `config/face_premium.php` defaults are valid, so only an operator override exposes the trap. Owner: config validation layer.
- Idempotency key regenerated on every `initiate()` call; after `request_checkout` compensation deletes the row, a fresh key is sent to Fedapay — `backend/app/Services/FaceSubscriptionPaymentService.php:104`. Correlated with the `request_checkout` ambiguity decision (Pass 2 D1). Owner: tied to the decision on that item.
- `throttle:30,1` on payment-initiation route is generous for an endpoint that writes DB + makes external HTTP — `backend/routes/api/face.php:152`. Bounded threat, monitoring concern. Owner: revisit if Fedapay rate-limit alerts appear.
- Cross-domain webhook isolation test instantiates `MissionPayment` directly rather than through `MissionPaymentService` — `backend/tests/Feature/Face/SubscriptionPaymentWebhookTest.php`. Does not verify bidirectional routing collision (a payload that satisfies both MissionPayment and FaceSubscription routing branches). Owner: test coverage hardening.
- `assertNotEmpty($response->json('data.subscription_id'))` does not validate UUID shape — `backend/tests/Feature/Face/SubscriptionPaymentInitiationTest.php:937`. A regression replacing UUID with auto-increment id would silently pass. Owner: test polish.
- Webhook replay test uses distinct envelope `event_id`s (`'evt_replay_1'`, `'evt_replay_2'`) to exercise row-level idempotency, bypassing the envelope-level dedupe — `backend/tests/Feature/Face/SubscriptionPaymentWebhookTest.php:1725`. The more common Fedapay retry case (same envelope id) is covered elsewhere by the envelope dedupe in `FedapayWebhookController:58`. Owner: defensive coverage.
- Fedapay customer payload sends `firstname` only, no `lastname` — `backend/app/Services/FedapayService.php:134-137`. Pattern mirrors booking/mission flows. Owner: cross-cutting Fedapay-payload review.
- D4 — Cancelled-but-not-yet-expired re-purchase does not chain (treat cancel = forfeit) — `backend/app/Services/FaceSubscriptionPaymentService.php:194-203`. A Face that cancels with `expires_at > now()` and immediately re-buys gets `starts_at = now()` instead of chaining from the cancelled row's `expires_at`. Confirmed as intentional 2026-05-12 (cancel = explicit user opt-out; chained re-purchase would muddy cancel semantics). Owner: document in Face premium product copy (no code change).

## Deferred from: code review Rerun 3 of feature-fp-1-5-annual-payment-initiation-and-idempotent-activation (2026-05-13)

- Strict amount-mismatch refusal leaves the Face charged with no entitlement and no automated recovery surface — `backend/app/Services/FaceSubscriptionPaymentService.php:217-248`. The customer paid (Fedapay confirmed), but `markAsPaid` keeps the row PendingPayment with `amount_mismatch_detected_at` in metadata. Row-hygiene is covered by existing orphan-pending sweep defers; the customer-facing dimension (refund decision, retry instructions, support escalation runbook) is not. Owner: ops/admin tooling story for amount-mismatch refund pipeline.
- Story spec text drift — multiple ACs no longer match the implemented behaviour (AC #6 "compensate-delete" vs preserve-row, AC #7 admin-message factory vs `pendingPaymentExistsForFace()`, AC #13 return type `int` vs `?int`, AC #14 signature widening + `addYear()` vs `addDays(365)`, AC #4 metadata shape at row creation). All are approved deviations from Pass 2 patches but the spec text on disk still describes the pre-patch behaviour. Owner: story-file refresh pass (no code change).

## Deferred from: code review of feature-fp-1-6-featured-placement-driven-by-active-subscription (2026-05-13)

- Extended N+1 hazard on admin-viewing nested `FaceResource` paths — `backend/app/Http/Resources/CandidatureResource.php:30`, `backend/app/Http/Resources/UserResource.php:66`. The new admin-only `is_featured_by_subscription` field calls `FaceEntitlementService::isFeaturedBySubscription` per Face. On admin paths that render `FaceResource` indirectly without preloading `activeSubscription` (admin candidature listings, admin user-show wrapping a Face), each Face now triggers 3 entitlement-service queries (`isPremium` + `publicAlbumPhotoLimit` + `isFeaturedBySubscription`) instead of 2. Pre-existing hazard tracked by `deferred-work.md:9` slightly worsened for the admin viewer only. Fix path: eager-load `activeSubscription` (and ideally chain through Candidature → Face) wherever admin renders nested `FaceResource`. Owner: FP-1.10 hardening pass, alongside the existing entitlement-service eager-load contract defer.
- Index column coverage on the EXISTS subquery — `backend/database/migrations/2026_05_11_083606_create_face_subscriptions_table.php:32`. The composite index `(face_id, status, expires_at)` covers three of the four filter columns; `plan` is read from the row. Selectivity on `status = active` keeps the per-Face cost bounded in practice, but if the active-subscriber population grows large or a second plan ships, consider extending the index to `(face_id, status, plan, expires_at)` or adding a covering index. Owner: revisit when paid subscriber count crosses a low-thousands threshold.

## Deferred from: code review of feature-fp-1-8-expiration-command-and-entitlement-removal (2026-05-14)

- Active subscriptions with null `expires_at` remain permanently `Active` because `subscriptions:expire-faces` only selects `expires_at <= now()`. Existing entitlement reads treat `Active + null expires_at` as inactive, so this is a pre-existing nullable-schema/corrupt-data hardening gap rather than the normal expiry path. Decide in FP-1.10 whether corrupt active-null rows should be expired, surfaced in a repair command, or blocked at write time — `backend/app/Console/Commands/ExpireFaceSubscriptionsCommand.php:32`, `backend/database/migrations/2026_05_11_083606_create_face_subscriptions_table.php`.

## Deferred from: code review of feature-fp-1-10-regression-coverage-and-rollout-backfill-safeguards (2026-05-19)

- Sprint-status note's "byte-level no-writes pin" wording overstates the assertion — `assertEquals` over `->get()->toArray()` is row-level deep-equality, not byte-level. Cosmetic metadata; no functional impact. Source: `_bmad-output/implementation-artifacts/sprint-status.yaml:4`.
- `acting_video` empty-string vs NULL handling in audit Section C — `whereNotNull('acting_video')` would include empty strings if Face form-request validation does not reject them. Verify Face form-request validation and either tighten it or add `->where('acting_video', '!=', '')` to the audit query. Source: `backend/app/Console/Commands/AuditFacePremiumReadinessCommand.php:68`.
- `has('photos', '>', 2)` audit Section B query does not check positions 3 & 4 specifically — minor false-positive for the Section B count if a Face has photos at non-contiguous positions (e.g. [1, 2, 5]). Position gaps are not allowed by FP-1.2 schema convention, but no DB-level constraint exists. Fix path: rewrite as `whereHas('photos', fn ($q) => $q->whereIn('position', [3, 4]))` and pair with a separate hardening story to add a CHECK constraint. Source: `backend/app/Console/Commands/AuditFacePremiumReadinessCommand.php:45`.
- Admin-cancel regression test does not verify the audit row was written — out of AC #12 scope (the pin was no-storage-delete). Adding `assertDatabaseHas('face_subscription_audits', ['action' => 'cancel', 'notes' => '...'])` would tighten the admin-cancel contract. Owner: future hardening pass on admin operations coverage. Source: `backend/tests/Feature/Subscription/FaceSubscriptionRegressionMatrixTest.php:530-572`.
- No regression-matrix coverage for chained-renewal (one Expired + one Active row on the same Face) — the audit command test covers this fixture (line 46-58 of `AuditFacePremiumReadinessCommandTest`) but the visibility matrix does not. A regression where `qualifiesAsPremium` resolution accidentally picks the Expired row would not be caught. Linked with `deferred-work.md:269` (asymmetric `qualifiesAsPremium`). Source: `backend/tests/Feature/Subscription/FaceSubscriptionRegressionMatrixTest.php`.
- No regression-matrix coverage for `presentation_video` field — out of FP-1.X scope (no entitlement contract on presentation video). Surface only if a future product decision gates the presentation video behind premium.
- Runbook smoke test #4 `<face-token>` placeholder lacks minting guidance — the on-call cannot run the smoke test without knowing how to obtain a token. Add a 1-line tinker snippet showing how to mint a Face token. Source: `docs/runbook-face-premium-rollout.md:220`.
- Runbook release-bundle table uses 7-char SHAs and a "this PR" placeholder for FP-1.10 — finalize post-merge by updating the FP-1.10 row with the actual merge SHA, or switch the table to signed tags (`fp-1.1`, `fp-1.2`, …) for stability across long-term git history growth. Source: `docs/runbook-face-premium-rollout.md:318-327`.
- `expectsOutputToContain` is substring-match, not equality — risk is theoretical at current fixture count magnitudes (asserting `: 3` would also match `: 30`). Switch to `Artisan::call() + Artisan::output() + assertSame` once multi-digit counts are exercised, or add complementary `doesntExpectOutputToContain` for adjacent values. Source: `backend/tests/Feature/Commands/AuditFacePremiumReadinessCommandTest.php:70-73,109,132,158,159`.
- Audit Section D anomalies lack a `--detailed` enumeration path — Section D outputs counts only; the runbook tells the operator to query manually (Section 7, line 198). Adding `--detailed` enumeration for Section D anomalies would be an ergonomic improvement. Pair with the same future hardening pass that fixes the data-hygiene defers themselves (`deferred-work.md:334`). Source: `backend/app/Console/Commands/AuditFacePremiumReadinessCommand.php:86-100`.

## Deferred from: code review of feature-fp-1-11-admin-subscription-management-ui (2026-05-20, re-run)

- AC #17 wording vs implemented behavior: story text mandates a test named `fetchSubscriptions handles error and resets subscriptions to empty array`, but a prior in-review patch (story line 601, `[Review][Patch]`) explicitly directed keeping last-known-good state on transient errors. The spec renames the case to `handles error without clearing existing subscriptions` and asserts the preserved list. Documented drift — AC text is the stale surface, runtime is the authorized one. No action needed beyond eventually editing the AC text in the next story-edit pass. Source: `frontend/src/features/admin/composables/__tests__/useAdminFaceSubscriptions.spec.ts:135-149`, `feature-fp-1-11-admin-subscription-management-ui.md:601`.
- ~~`isLoading` shared between fetch and the 4 mutation methods~~ — **resolved 2026-05-20 in closeout claude-review**: the 4 mutation methods (`activate` / `extend` / `cancel` / `correct`) no longer toggle `isLoading`; each modal owns its own `*Submitting` ref for button-disable, so the section list no longer unmounts under the open modal. Inline comment added on `useAdminFaceSubscriptions.ts` to document the invariant. Source: `frontend/src/features/admin/composables/useAdminFaceSubscriptions.ts:120-198`.
- `AdminFaceSubscriptionSection.vue` is 1236 lines with 4 near-identical modals (activate / extend / cancel / correct each duplicate header / X / conflict-banner / footer pattern). Single-component maintenance is brittle — any modal-level patch (e.g. the ARIA fix from this review) must be applied 4 times. Schedule extraction of `<AdminSubscriptionModal>` with slots in a follow-up cleanup story. Source: `frontend/src/features/admin/components/AdminFaceSubscriptionSection.vue:743-1213`.
- No integration test exercises composable + service together without mocks — every existing spec mocks one layer, so wiring regressions (endpoint path drift, payload key swap, response shape drift) could ship undetected. Schedule an MSW-based integration spec. Source: `frontend/src/features/admin/composables/__tests__/useAdminFaceSubscriptions.spec.ts`.
- Audit JSON `previous_state` / `new_state` is rendered raw in `<pre>` with no truncation / virtualization — a sub with 50+ audits expanded can produce kilobytes of JSON on screen and slow the browser. Add truncation + "Voir le JSON complet" disclosure when audit volume warrants. Source: `frontend/src/features/admin/components/AdminFaceSubscriptionSection.vue:721-728`.

## Deferred from: code review of feature-fp-1-11-admin-subscription-management-ui (2026-05-20, 3-agent pass)

- `submitCorrect` "no changes" guard blocks notes-only updates — Correct modal rejects submit with `"Modifiez au moins une des deux dates"` when neither date moved, so the operator cannot amend the audit note alone via this modal. Operator accepted as intentional UX (Correct is a date-correction tool). If product later wants notes-only edits, either lift the guard and POST without date fields (verify backend FP-1.4 contract) or add a separate "Edit notes" modal. Source: `frontend/src/features/admin/components/AdminFaceSubscriptionSection.vue:479-485`.
- Modal mutual-exclusion guard absent — `openExtend` / `openCancel` / `openCorrect` lack a `closeOthers()` precondition; sequential rapid-fire opens could mount two `<Teleport>` panels. No observed bug, UX guards (single trigger per row) make this very unlikely. Defensive cleanup only. Source: `frontend/src/features/admin/components/AdminFaceSubscriptionSection.vue:317,393,456`.
- `trapModalFocus` filters focusables by `offsetParent !== null` which is unreliable for `position: fixed` ancestors during the Transition entering frame. Modal buttons are not fixed so currently fine, but a refactor adding fixed children could silently break the trap. Source: `frontend/src/features/admin/components/AdminFaceSubscriptionSection.vue:195-217`.
- `is_featured` (legacy manual feature flag) is not surfaced in the admin Faces list — admin cannot distinguish manual-featured from subscription-featured at list level. Out of FP-1.11 scope (story only adds the Premium-by-subscription indicator). Schedule separately if product wants both flags exposed. Source: `frontend/src/pages/admin/AdminFacesListPage.vue:2650-2659`.
- No precedence rule between `Inactif` and `Premium` badges — an inactive Face with `is_featured_by_subscription: true` shows both side-by-side with no visual hierarchy. Out of FP-1.11 scope (no AC mandates precedence). Source: `frontend/src/pages/admin/AdminFacesListPage.vue:2644-2659`.
